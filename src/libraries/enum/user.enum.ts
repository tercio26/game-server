export enum Role {
    ADMIN_ACCOUNT = 0,
    ADMIN = 1,
    NORMAL = 2,
}

export enum UserStatus {
    ACTIVE = 2,
    INVITING = 1,
    INACTIVE = 0,
}

export enum AdminAccount {
    IS_ADMIN_ACCOUNT = 1,
    ADMIN_MEMBER = 0,
}

export enum UserProvider {
    LOCAL = 0,
    GOOGLE = 1,
    FACEBOOK = 2,
    APPlE_ID = 3,
}